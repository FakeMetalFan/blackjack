import { rank } from './rank';

export const ranks = [
  rank.Ace,
  rank.Two,
  rank.Three,
  rank.Four,
  rank.Five,
  rank.Six,
  rank.Seven,
  rank.Eight,
  rank.Nine,
  rank.Ten,
  rank.Jack,
  rank.Queen,
  rank.King,
];
